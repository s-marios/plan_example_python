planning_scene_monitor_options:
  name: "planning_scene_monitor"
  robot_description: "robot_description"
  joint_state_topic: "/joint_states"
  attached_collision_object_topic: "/moveit_cpp/planning_scene_monitor"
  publish_planning_scene_topic: "/moveit_cpp/publish_planning_scene"
  monitored_planning_scene_topic: "/moveit_cpp/monitored_planning_scene"
  wait_for_initial_state_timeout: 20.0

planning_pipelines:
  pipeline_names: ["ompl", "pilz_industrial_motion_planner", "stomp"]
  #pipeline_names: ["ompl", "pilz_industrial_motion_planner", "chomp", "stomp"]

plan_request_params:
  planning_attempts: 2
  planning_pipeline: ompl
  planner_id: "RRTConnectkConfigDefault"
  planning_time: 10.0
  max_velocity_scaling_factor: 0.2
  max_acceleration_scaling_factor: 0.2

ompl_rrtc:  # Namespace for individual plan request
  plan_request_params:  # PlanRequestParameters similar to the ones that are used by the single pipeline planning of moveit_cpp
    planning_attempts: 2  # Number of attempts the planning pipeline tries to solve a given motion planning problem
    planning_pipeline: ompl  # Name of the pipeline that is being used
    planner_id: "RRTConnectkConfigDefault"
    max_velocity_scaling_factor: 0.2  # Velocity scaling parameter for the trajectory generation algorithm that is called (if configured) after the path planning
    max_acceleration_scaling_factor: 0.2  # Acceleration scaling parameter for the trajectory generation algorithm that is called (if configured) after the path planning
    planning_time: 5.0  # Time budget for the motion plan request. If the planning problem cannot be solved within this time, an empty solution with error code is returned

pilz_ptp:
  plan_request_params:
    planning_attempts: 10
    planning_pipeline: pilz_industrial_motion_planner
    planner_id: "PTP"
    max_velocity_scaling_factor: 0.2
    max_acceleration_scaling_factor: 0.2
    planning_time: 0.5 #same time for all planners?

pilz_lin:
  plan_request_params:
    planning_attempts: 10
    planning_pipeline: pilz_industrial_motion_planner
    planner_id: "LIN"
    max_velocity_scaling_factor: 0.3
    max_acceleration_scaling_factor: 0.3
    planning_time: 0.5 #same time for all planners?

chomp_planner:
  plan_request_params:
    planning_attempts: 1
    planning_pipeline: chomp
    max_velocity_scaling_factor: 0.1
    max_acceleration_scaling_factor: 0.1
    planning_time: 5.0

ompl_rrt_star:
  plan_request_params:
    planning_attempts: 2
    planning_pipeline: ompl
    planner_id: "RRTstarkConfigDefault"
    max_velocity_scaling_factor: 0.2
    max_acceleration_scaling_factor: 0.2
    planning_time: 5.0

stomp_planner:
  plan_request_params:
    planning_attempts: 1
    planning_pipeline: stomp
    #planner_id: "not_sure"
    max_velocity_scaling_factor: 0.2
    max_acceleration_scaling_factor: 0.2
    planning_time: 10.0

default_request_adapter_parameters:
  fix_start_state: true
  default_workspace_bounds: 1.5
